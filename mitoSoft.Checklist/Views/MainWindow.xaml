<Window x:Class="mitoSoft.Checklist.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Checklist" 
        Height="800" Width="1400"
        MinHeight="600" MinWidth="1000"
        Background="White"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <!-- Desktop Mode Styles -->
        <Style x:Key="MainButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#F5F5F5"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="15,10"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#E0E0E0"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Opacity" Value="0.5"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="PrimaryButtonStyle" TargetType="Button" BasedOn="{StaticResource MainButtonStyle}">
            <Setter Property="Background" Value="#1E90FF"/>
            <Setter Property="Foreground" Value="White"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#1C7ED6"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Tablet Mode Styles -->
        <Style x:Key="MainButtonStyleTablet" TargetType="Button">
            <Setter Property="Background" Value="#F5F5F5"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="Padding" Value="20,15"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#E0E0E0"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Opacity" Value="0.5"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="PrimaryButtonStyleTablet" TargetType="Button" BasedOn="{StaticResource MainButtonStyleTablet}">
            <Setter Property="Background" Value="#1E90FF"/>
            <Setter Property="Foreground" Value="White"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#1C7ED6"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header Section -->
        <Grid Grid.Row="0" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Logo -->
            <Image x:Name="imgLogo" 
                   Grid.Column="0" 
                   Width="180"
                   Margin="10,0,20,0"
                   HorizontalAlignment="Left"
                   Stretch="Uniform" 
                   Source="/Resources/WVE.png"/>

            <!-- Buttons -->
            <StackPanel x:Name="buttonPanel" 
                       Grid.Column="1" 
                       Orientation="Horizontal" 
                       HorizontalAlignment="Left"
                       VerticalAlignment="Top" 
                       Height="40">
                <Button x:Name="toggleMode" 
                        Style="{StaticResource MainButtonStyle}"
                        Click="ToggleMode_Click" 
                        Content="💻"
                        ToolTip="Umschalten zwischen Tablet und Desktop Modus"/>
                <Button x:Name="btnLoadTemplate" 
                        Content="📋" 
                        Style="{StaticResource MainButtonStyle}"
                        Click="LoadTemplate_Clicked"
                        ToolTip="Vorlage laden"/>
                <Button x:Name="btnLoad" 
                        Content="📂" 
                        Style="{StaticResource MainButtonStyle}"
                        Click="Load_Clicked"
                        ToolTip="Laden"/>
                <Button x:Name="btnSave" 
                        Content="💾" 
                        Style="{StaticResource MainButtonStyle}"
                        IsEnabled="False"
                        Click="Save_Clicked"
                        ToolTip="Speichern"/>
                <Button x:Name="btnSaveAs" 
                        Content="➕" 
                        Style="{StaticResource MainButtonStyle}"
                        IsEnabled="False"
                        Click="SaveAs_Clicked"
                        ToolTip="Speichern unter..."/>
                <Button x:Name="btnExportPdf" 
                        Content="📄" 
                        Style="{StaticResource MainButtonStyle}"
                        IsEnabled="False"
                        Click="ExportPdf_Clicked"
                        ToolTip="PDF Export"/>
            </StackPanel>
        </Grid>

        <!-- Plan Title -->
        <TextBlock x:Name="lblPlanTitle" 
                   Grid.Row="1"
                   Text="Kein Plan geladen..."
                   FontSize="32" 
                   FontWeight="Bold"
                   Margin="0,0,0,20"/>

        <!-- Main Content -->
        <GroupBox x:Name="groupBoxSteps"
                  Grid.Row="2" 
                  Header="Wartungsschritte" 
                  FontSize="18"
                  Padding="10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Step Index -->
                <TextBlock x:Name="lblStepIndex" 
                           Grid.Row="0"
                           Text="steps" 
                           FontSize="16"
                           HorizontalAlignment="Right"
                           Margin="0,0,0,10"/>

                <!-- Description Label -->
                <TextBlock 
                           Text="Kurzbeschreibung:"
                           FontSize="16"
                           FontStyle="Italic"
                           Margin="0,14,0,34" Grid.RowSpan="2"/>

                <!-- Step Description -->
                <TextBlock x:Name="lblStepDescription" 
                           Grid.Row="1"
                           Text="Description"
                           FontSize="14"
                           Margin="0,11,0,29"
                           TextWrapping="Wrap"/>

                <!-- Tasks ScrollViewer -->
                <ScrollViewer Grid.Row="2" 
                             VerticalScrollBarVisibility="Auto"
                             Margin="0,10,0,10"
                             PanningMode="VerticalOnly"
                             PanningDeceleration="0.001"
                             PanningRatio="1">
                    <StackPanel x:Name="spTasks" 
                               Background="White"/>
                </ScrollViewer>

                <!-- Navigation Buttons -->
                <Grid Grid.Row="3" Margin="0,10,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <Button x:Name="btnBack" 
                            Grid.Column="0"
                            Content="◀" 
                            Style="{StaticResource PrimaryButtonStyle}"
                            IsEnabled="False"                            
                            Click="Back_Clicked"
                            ToolTip="Zurück"/>

                    <Button x:Name="btnNext" 
                            Grid.Column="2"
                            Content="▶" 
                            Style="{StaticResource PrimaryButtonStyle}"
                            IsEnabled="False"
                            Click="Next_Clicked"
                            ToolTip="Weiter"/>
                </Grid>
            </Grid>
        </GroupBox>

        <!-- Status Bar -->
        <StatusBar Grid.Row="3" Margin="0,10,0,0">
            <StatusBarItem Background="White">
                <TextBlock x:Name="lblStatus" Text=""
                           Background="White"
                           FontSize="14"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>
